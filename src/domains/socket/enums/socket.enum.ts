export enum IoClientEvent {
  UPDATE_PRICES = 'update-prices',
}

export enum IoClientRequest {
  SUBSCRIBE = 'subscribe',
}

export enum IoClientMessage {
  JOIN_PRICES_ROOM = 'ioToken.joinUpdatePrices',
}
